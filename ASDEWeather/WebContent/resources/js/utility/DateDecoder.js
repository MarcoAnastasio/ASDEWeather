/**
 * 
 */
/*function dateDecoder(date_input){
	this.date=date_input;
	var substrings = [];
     substrings = date_input.split(" ");
     
     var dates=[];
         dates=substrings[0].split("-");
     
      var times=[];
      times=substrings[1].split(":");
      
      var day=dates[2];
      var month=dates[1]
      var year=dates[0];
      var hour=times[0];
      var minute=times[1];
      
      var day_name=DayNamer(month +"/"+day+"/"+year); 
      
      
      
     this.day=day;
     this.month=month;
     this.year=year;
     this.hour=hour;
     this.minute=minute;
     this.day_name=day_name;
     foreach (substring in substrings){
    	// Console.WriteLine(substring); 
     }
}*/

